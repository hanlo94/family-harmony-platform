/**
 * API request wrapper based on uni.request
 * Handles token injection, error handling, and token refresh
 */

// Placeholder - will be implemented in C06-4
export const request = (options: Record<string, unknown>) => {
  return uni.request(options as UniApp.RequestOptions);
};
