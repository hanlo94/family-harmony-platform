# UI 设计文档

**版本**：v1.0  
**日期**：2026-06-10  
**依赖**：[requirements.md](./requirements.md)、[architecture.md](./architecture.md)  
**平台**：移动端 H5（微信公众号内打开，375-414px 视口为主，后续编译微信小程序）

---

## 1. 设计理念

### 1.1 核心原则

| 原则 | 说明 |
|------|------|
| **一眼看懂** | 打开 App 就知道今天要做什么，不需要教程 |
| **温暖不幼稚** | 大人和孩子都觉得舒服，不偏向任何年龄段 |
| **动效有意义** | 完成任务的动画让人有成就感，不是为了炫技 |
| **减少摩擦** | 3 步内完成核心操作，表单字段越少越好 |

### 1.2 情绪板关键词

`温暖` `轻量` `和谐` `可靠` `小确幸`

---

## 2. Design Token 系统

### 2.1 配色方案 — "Sunlit Kitchen Garden"

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Forest        Peach         Honey         Moss         │
│  #3D6B5A       #E8A87C       #F4D35E       #8CB89F     │
│  ■■■■■■■■■■   ■■■■■■■■■■    ■■■■■■■■■■   ■■■■■■■■■■  │
│  主色/导航      强调/CTA      高亮/星级     完成/成功     │
│                                                         │
│  Paper         Ink           Sand          Rose         │
│  #F9F8F4       #1A1A1A       #F5EDE4       #E8A4A0     │
│  ■■■■■■■■■■   ■■■■■■■■■■    ■■■■■■■■■■   ■■■■■■■■■■  │
│  页面背景       正文文字       卡片背景       错误/驳回     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

| Token | 色值 | 用途 |
|-------|------|------|
| `--color-primary` | `#3D6B5A` | 主按钮、导航栏、图标、Tab 选中态 |
| `--color-accent` | `#E8A87C` | CTA 按钮（创建任务）、重要操作 |
| `--color-honey` | `#F4D35E` | 难度星级、徽章、签到高亮 |
| `--color-moss` | `#8CB89F` | 已完成状态、成功 Toast、进度条 |
| `--color-bg` | `#F9F8F4` | 页面背景 |
| `--color-surface` | `#F5EDE4` | 卡片、列表项背景 |
| `--color-text` | `#1A1A1A` | 正文文字 |
| `--color-text-secondary` | `#6B6B6B` | 次要文字、时间戳 |
| `--color-rose` | `#E8A4A0` | 错误提示、驳回标签、逾期标记 |
| `--color-border` | `#E8E3DC` | 分割线、边框 |
| `--color-white` | `#FFFFFF` | 纯白卡片、输入框背景 |

### 2.2 字体

移动端 H5 + 小程序，优先使用系统字体以保证加载速度和原生体验：

```css
/* 中文正文字体栈 */
--font-body: -apple-system, "PingFang SC", "Hiragino Sans GB",
             "Microsoft YaHei", "Helvetica Neue", sans-serif;

/* 数字/英文显示字体（用于时间、计数、难度数字） */
--font-display: "SF Pro Rounded", "DIN Alternate", -apple-system,
                "PingFang SC", sans-serif;
```

**字体层级**：

| 级别 | 大小 | 字重 | 用途 |
|------|------|------|------|
| H1 | 22px / Bold (700) | 页面标题（如"家庭任务"） |
| H2 | 18px / Semibold (600) | 区块标题（如"待完成"） |
| Body | 16px / Regular (400) | 正文、任务标题 |
| Caption | 13px / Regular (400) | 辅助信息、时间、成员名 |
| Small | 11px / Medium (500) | 标签、徽章 |

### 2.3 间距

基于 4px 栅格，移动端适配：

| Token | 值 | 用途 |
|-------|-----|------|
| `--space-xs` | 4px | 图标与文字间距 |
| `--space-sm` | 8px | 标签内边距 |
| `--space-md` | 12px | 列表项内边距 |
| `--space-lg` | 16px | 卡片内边距、页面水平边距 |
| `--space-xl` | 20px | 区块间距 |
| `--space-2xl` | 24px | 页面顶部/底部留白 |

### 2.4 圆角

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-sm` | 6px | 标签、徽章、小按钮 |
| `--radius-md` | 10px | 卡片、列表项、输入框 |
| `--radius-lg` | 14px | 大按钮（主 CTA） |
| `--radius-full` | 9999px | 头像、药丸形标签 |

### 2.5 阴影

移动端阴影克制使用，主要通过颜色区分层级：

| Token | 值 | 用途 |
|-------|-----|------|
| `--shadow-card` | `0 1px 3px rgba(0,0,0,0.06)` | 卡片浮起 |
| `--shadow-fab` | `0 2px 8px rgba(0,0,0,0.12)` | 浮动操作按钮 |

---

## 3. 页面清单与路由

```
┌──────────────────────────────────────────────────────────┐
│                      App Shell                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  📋 任务  │  │  👨‍👩‍👧 家庭 │  │  👤 我的  │   ← Bottom   │
│  │  (首页)   │  │          │  │          │     TabBar   │
│  └──────────┘  └──────────┘  └──────────┘              │
└──────────────────────────────────────────────────────────┘
```

| 页面 | 路由 | 说明 |
|------|------|------|
| 任务首页 | `/pages/index/index` | TabBar 首页，展示当前家庭任务列表，按状态筛选 |
| 任务详情 | `/pages/task/detail?id=xxx` | 查看任务完整信息、操作按钮 |
| 创建任务 | `/pages/task/create` | 新建任务表单 |
| 验收任务 | `/pages/task/verify?id=xxx` | 组织者验收（看照片 + 通过/驳回） |
| 家庭管理 | `/pages/family/index` | TabBar 第二个 Tab，成员列表、邀请入口 |
| 邀请成员 | `/pages/family/invite` | 生成邀请链接/二维码 |
| 加入家庭 | `/pages/family/join?code=xxx` | 被邀请者入口 |
| 个人中心 | `/pages/profile/index` | TabBar 第三个 Tab，我的资料、切换家庭、设置 |
| 登录页 | `/pages/login/index` | 微信登录入口 |

---

## 4. 页面设计

### 4.1 任务首页（最重要）

```
┌────────────────────────────────────┐
│  Status Bar                        │
├────────────────────────────────────┤
│  幸福三口之家          [切换家庭 ▾] │  ← Header (Forest 背景)
├────────────────────────────────────┤
│  ┌──────────┬──────────┬────────┐ │
│  │ ● 待完成  │  待验收   │  已完成  │ │  ← 状态筛选 Tab
│  │   (3)    │   (1)    │  (12)   │ │     圆点颜色对应状态
│  └──────────┴──────────┴────────┘ │
│                                    │
│  ┌─ 今天 ───────────────────────┐ │
│  │                              │ │
│  │  ┌────────────────────────┐  │ │
│  │  │ 🧹 今天洗碗            │  │ │  ← 任务卡片
│  │  │ ⭐⭐        ⏰ 20:00   │  │ │     左侧难度星级
│  │  │ 👤 小明爸爸            │  │ │     右侧截止时间
│  │  │ ┌──────────────┐      │  │ │
│  │  │ │  标记完成 →  │      │  │ │  ← CTA 按钮 (Peach)
│  │  │ └──────────────┘      │  │ │
│  │  └────────────────────────┘  │ │
│  │                              │ │
│  │  ┌────────────────────────┐  │ │
│  │  │ 🧺 洗衣服              │  │ │
│  │  │ ⭐⭐⭐      ⏰ 明天    │  │ │
│  │  │ 👤 我                  │  │ │
│  │  │ ┌──────────────┐      │  │ │
│  │  │ │  标记完成 →  │      │  │ │
│  │  │ └──────────────┘      │  │ │
│  │  └────────────────────────┘  │ │
│  │                              │ │
│  └──────────────────────────────┘ │
│                                    │
│                              ┌────┐│
│                              │ ＋ ││  ← FAB 创建任务
│                              └────┘│    (Peach 圆形浮动按钮)
└────────────────────────────────────┘
```

**交互说明**：
- 顶部 Tab 切换时，下方列表平滑过渡（translateX + opacity）
- 点击"标记完成"按钮后，卡片向右滑出，出现 ✅ 动画
- 下拉刷新重新加载数据
- 空状态：显示插画 + "今天没有待完成的任务，太棒了！"
- FAB 按钮固定右下角，页面滚动时不随动（position: fixed）

### 4.2 任务详情页

```
┌────────────────────────────────────┐
│  ← 返回         任务详情           │
├────────────────────────────────────┤
│                                    │
│  ┌──────────────────────────────┐  │
│  │                              │  │
│  │  今天洗碗                     │  │
│  │  难度：⭐⭐                    │  │
│  │                              │  │
│  │  📝 任务描述                  │  │
│  │  用洗洁精认真洗，洗完擦干       │  │
│  │  放回原位                     │  │
│  │                              │  │
│  │  ⏰ 截止时间                  │  │
│  │  2026年6月10日 20:00         │  │
│  │                              │  │
│  │  👤 执行人：小明爸爸           │  │
│  │  📌 状态：待完成               │  │
│  │                              │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌──────────────────────────────┐  │
│  │        📸 标记完成            │  │  ← 大按钮 (Peach)
│  │       （可上传照片）           │  │
│  └──────────────────────────────┘  │
│                                    │
│  [组织者视图额外显示]               │
│  [编辑任务]  [取消任务]  ← 次要操作  │
└────────────────────────────────────┘
```

### 4.3 创建任务页

```
┌────────────────────────────────────┐
│  ← 返回         创建任务           │
├────────────────────────────────────┤
│                                    │
│  任务标题 *                         │
│  ┌──────────────────────────────┐  │
│  │ 输入任务名称，如"今天洗碗"      │  │
│  └──────────────────────────────┘  │
│                                    │
│  任务描述                           │
│  ┌──────────────────────────────┐  │
│  │ 选填，补充说明要求...          │  │
│  │                              │  │
│  └──────────────────────────────┘  │
│                                    │
│  难度                              │
│  ☆ ☆ ☆ ☆ ☆    ← 点击选星级        │
│                                    │
│  截止时间                           │
│  ┌──────────────────────────────┐  │
│  │ 📅 2026年6月10日 20:00  ▼    │  │  ← 时间选择器
│  └──────────────────────────────┘  │
│                                    │
│  分配给                             │
│  ┌──────────────────────────────┐  │
│  │ 👤 选择家庭成员          ▼    │  │  ← 成员选择器
│  └──────────────────────────────┘  │
│                                    │
│  ┌──────────────────────────────┐  │
│  │         ✓ 创建任务            │  │  ← 提交按钮 (Forest)
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

**交互说明**：
- 难度星级：点击第 N 颗星，前 N 颗亮起 Honey 色，有缩放动画
- 时间选择：使用 uni-app `<picker mode="date">`，移动端原生体验
- 创建成功后 Toast "任务已创建" → 自动跳回首页

### 4.4 验收页（组织者专用）

```
┌────────────────────────────────────┐
│  ← 返回         验收任务           │
├────────────────────────────────────┤
│                                    │
│  小明爸爸 完成了「今天洗碗」         │
│                                    │
│  ┌──────────────────────────────┐  │
│  │                              │  │
│  │      [完成照片预览]           │  │
│  │      点击可放大              │  │
│  │                              │  │
│  └──────────────────────────────┘  │
│                                    │
│  完成时间：2026年6月10日 18:30     │
│                                    │
│  ┌────────────┐  ┌────────────┐   │
│  │  ✓ 验收通过 │  │  ✗ 驳回    │   │
│  │  (Moss)    │  │  (Rose)    │   │
│  └────────────┘  └────────────┘   │
│                                    │
│  [驳回时弹出输入框]                  │
│  ┌──────────────────────────────┐  │
│  │ 请说明驳回原因...             │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

### 4.5 家庭管理页

```
┌────────────────────────────────────┐
│  我的家庭                          │
├────────────────────────────────────┤
│                                    │
│  当前家庭：幸福三口之家             │
│  邀请码：ABC123                    │
│                                    │
│  ┌─ 家庭成员 (3人) ─────────────┐  │
│  │                              │  │
│  │  👤 小明妈妈  [组织者]        │  │
│  │  👤 小明爸爸  [成员]          │  │
│  │  👤 小明      [成员]          │  │
│  │                              │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌──────────────────────────────┐  │
│  │      📨 邀请新成员            │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌──────────────────────────────┐  │
│  │      🏠 切换到其他家庭         │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

### 4.6 个人中心页

```
┌────────────────────────────────────┐
│  我的                             │
├────────────────────────────────────┤
│  ┌──────────────────────────────┐  │
│  │  👤 头像   小明妈妈           │  │
│  │          微信号：mama_xxx     │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌─ 设置 ───────────────────────┐  │
│  │  提醒设置              >     │  │
│  │  关于我们              >     │  │
│  │  退出登录              >     │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

---

## 5. 组件树

```
App.vue
├── AuthGuard.vue                  # 登录状态检查，未登录跳转登录页
│
├── pages/index/index.vue          # 首页 — 任务列表
│   ├── TaskFilter.vue             #   状态筛选 Tab 栏
│   ├── TaskCard.vue               #   任务卡片（循环渲染）
│   │   └── StarRating.vue         #     难度星级
│   ├── EmptyState.vue             #   空状态插画
│   └── FloatingActionButton.vue   #   创建任务 FAB
│
├── pages/task/detail.vue          # 任务详情
│   ├── TaskInfoCard.vue           #   任务信息区
│   ├── CompleteButton.vue         #   标记完成按钮
│   ├── PhotoUploader.vue          #   照片上传
│   └── OrgActions.vue             #   组织者操作（编辑/取消）
│
├── pages/task/create.vue          # 创建任务
│   ├── TaskForm.vue               #   表单容器
│   ├── StarInput.vue              #   星级选择器
│   ├── MemberPicker.vue           #   成员下拉选择
│   └── DateTimePicker.vue         #   日期时间选择
│
├── pages/task/verify.vue          # 验收任务
│   ├── PhotoPreview.vue           #   完成照片放大
│   └── VerifyActions.vue          #   通过/驳回按钮组
│
├── pages/family/index.vue         # 家庭管理
│   ├── MemberList.vue             #   成员列表
│   └── InviteAction.vue           #   邀请入口
│
├── pages/family/invite.vue        # 邀请成员
│   └── InviteCard.vue             #   邀请码展示+分享
│
├── pages/family/join.vue          # 加入家庭
│   └── JoinForm.vue               #   输入邀请码
│
├── pages/profile/index.vue        # 个人中心
│   └── SettingsList.vue           #   设置列表
│
└── pages/login/index.vue          # 登录页
    └── WechatLoginButton.vue      #   微信登录按钮
```

---

## 6. 状态管理 (Pinia)

```
stores/
├── auth.ts      # 登录状态
│   ├── token: string | null        # JWT Token
│   ├── user: UserProfile | null    # 当前用户
│   ├── isLoggedIn: boolean
│   └── actions: login(), logout(), refreshToken()

├── family.ts    # 家庭状态
│   ├── currentFamily: Family | null  # 当前选中的家庭
│   ├── families: Family[]            # 用户的所有家庭
│   ├── members: Member[]             # 当前家庭成员
│   └── actions: switchFamily(), loadMembers()

└── task.ts      # 任务状态
    ├── tasks: Task[]                 # 当前列表
    ├── filter: TaskStatus | 'ALL'    # 当前筛选状态
    ├── pagination: { page, total }
    └── actions: loadTasks(), completeTask(), verifyTask()
```

---

## 7. 动效设计

| 场景 | 动效 | 时机 | 时长 |
|------|------|------|------|
| **任务完成** | 卡片向右滑出 + 绿色对勾缩放出现 | 点击"标记完成"后 | 400ms |
| **验收通过** | 卡片背景变 Moss 绿 + 轻微弹跳 | 点击"验收通过"后 | 500ms |
| **驳回** | 卡片微颤（shake）+ 驳回原因淡入 | 驳回操作 | 300ms |
| **创建任务** | FAB 按钮缩放消失 → 表单从底部滑入 | 点击 FAB | 250ms |
| **Tab 切换** | 内容左右滑动（translateX）+ 底横线跟随 | 切换筛选 Tab | 200ms |
| **列表加载** | 卡片从下往上依次淡入（stagger） | 首次加载 | 每张 80ms 间隔 |
| **下拉刷新** | 顶部加载指示器旋转 + "松手刷新" | 下拉 | — |
| **Toast** | 从顶部滑入 → 2s 后滑出 | 操作反馈 | 300ms |
| **空状态** | 插画轻微浮动（translateY 循环） | 页面空闲 | 3s 循环 |

### 动效原则
- 所有动效尊重 `prefers-reduced-motion`，用户开启减少动效时禁用
- 使用 CSS `transition` / `animation` 而非 JS 动画库，确保 60fps
- 微信小程序版使用 uni-app 的 `<transition>` 组件替代 CSS transition

---

## 8. 响应式策略

| 断点 | 视口宽度 | 适配 |
|------|----------|------|
| 手机竖屏 | 320-414px | 主设计基准（设计稿 375px） |
| 手机横屏 | 568-812px | Tab 筛选改为水平滚动，卡片最大宽度 480px 居中 |
| 平板 | 768px+ | 卡片最大宽度 600px 居中，双列网格 |

使用 `rpx` 单位（uni-app 的响应式单位，750rpx = 屏幕宽度），小程序和 H5 自动适配。

---

## 9. 视觉签名（Signature Element）

> **完成时刻的"小确幸"动画**：当成员标记任务完成时，屏幕中央短暂出现一个金色星星（Honey 色）带着粒子效果绽放，持续约 1 秒。这不是游戏化的积分系统，而是一个纯粹的情感反馈——"你为家庭做了贡献，这很重要"。
>
> 这个星星是整个 App 的视觉签名，出现在：
> - 任务完成时（大颗，中央绽放）
> - 验收通过时（小颗，卡片上方）
> - 空状态时（静态，插画中点缀）
