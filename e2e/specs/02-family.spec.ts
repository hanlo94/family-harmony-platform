/**
 * E2E: 家庭管理
 *
 * 场景：
 * 1. 创建家庭
 * 2. 生成邀请码
 * 3. 另一用户通过邀请码加入
 * 4. 查看家庭成员列表
 * 5. 移除成员
 */
import { test, expect } from '@playwright/test';

test.describe('Family Management', () => {
  test('should create a family', async ({ page }) => {
    test.skip();
    // TODO
  });

  test('should generate invite code', async ({ page }) => {
    test.skip();
    // TODO
  });

  test('should join family via invite code', async ({ page }) => {
    test.skip();
    // TODO
  });

  test('should show member list with roles', async ({ page }) => {
    test.skip();
    // TODO
  });
});
